apiVersion: v1
kind: Service
metadata:
  labels:
    app: pgsql
  name: db
  namespace: j23zebia
spec:
  type: ClusterIP
  ports:
  - name: pgsql-svc-port
    port: 5432
    targetPort: postgres-port
  selector:
    app: pgsql
